"""
FastAPI app entry point. Keeps startup config minimal — create the app,
wire up the routes, make sure the database tables exist before we start
taking requests.
"""

# implementation coming in Phase 0c
